import styled from 'styled-components';

export const Form = styled.form`
  display: flex;
  flex: 1;
  justify-content: space-between;

  input {
    flex: 1;
    margin-right: 20px;
  }

  button {
    width: 60px;
  }
`;
